import '../../lib/gl-matrix';

var glm = {
  glMatrix: window.glMatrix,
  vec2: window.vec2,
  vec3: window.vec3,
  vec4: window.vec4,
  mat2: window.mat2,
  mat2d: window.mat2d,
  mat3: window.mat3,
  mat4: window.mat4,
  quat: window.quat
};

export default glm;
